import ComingSoon from "@/components/common/ComingSoon";

// Temporary placeholder — the real page is preserved as `page.original.tsx`
// in this folder. To restore: delete this file and rename
// `page.original.tsx` back to `page.tsx`.
export default function BuyInsurancePage() {
  return (
    <ComingSoon
      title="Insurance"
      description="Insurance quotes and purchases are temporarily paused while we ship an upgraded version."
    />
  );
}
